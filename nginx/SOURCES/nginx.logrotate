/var/log/nginx/*.log
/var/log/nginx/*/*.log {
    daily
    missingok
    rotate 62
    compress
    delaycompress
    notifempty
    create 640 nginx root
    sharedscripts
    postrotate
        [ -f /var/run/nginx.pid ] && kill -USR1 `cat /var/run/nginx.pid`
    endscript
}

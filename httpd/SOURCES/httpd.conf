###############################################################################

# Provides authentication front-ends such as mod_auth_digest and mod_auth_basic
LoadModule authn_file_module /usr/lib64/httpd/modules/mod_authn_file.so

# Provides authentication front-ends such as mod_auth_digest and mod_auth_basic
#LoadModule authn_dbm_module /usr/lib64/httpd/modules/mod_authn_dbm.so

# Provides authentication front-ends such as mod_auth_basic to authenticate
# users similar to anonymous-ftp sites, i.e. have a 'magic' user id 'anonymous'
# and the email address as a password.
#LoadModule authn_anon_module /usr/lib64/httpd/modules/mod_authn_anon.so

# Provides authentication front-ends such as mod_auth_digest and mod_auth_basic
# to authenticate users by looking up users in SQL tables
#LoadModule authn_dbd_module /usr/lib64/httpd/modules/mod_authn_dbd.so

# Maintains a cache of authentication credentials, so that a new backend
# lookup is not required for every authenticated request.
#LoadModule authn_socache_module /usr/lib64/httpd/modules/mod_authn_socache.so

# Provides core authentication capabilities to allow or deny access to
# portions of the web site
LoadModule authn_core_module /usr/lib64/httpd/modules/mod_authn_core.so

# Provides core authorization capabilities so that authenticated users can
# be allowed or denied access to portions of the web site
LoadModule authz_host_module /usr/lib64/httpd/modules/mod_authz_host.so

# Provides authorization capabilities so that authenticated users can be
# allowed or denied access to portions of the web site by group membership.
LoadModule authz_groupfile_module /usr/lib64/httpd/modules/mod_authz_groupfile.so

# Provides authorization capabilities so that authenticated users can be
# allowed or denied access to portions of the web site.
LoadModule authz_user_module /usr/lib64/httpd/modules/mod_authz_user.so

# Provides authorization capabilities so that authenticated users can be
# allowed or denied access to portions of the web site by group membership.
# Similar functionality is provided by mod_authz_groupfile.
#LoadModule authz_dbm_module /usr/lib64/httpd/modules/mod_authz_dbm.so

# Authorizes access to files by comparing the userid used for HTTP
# authentication (the web userid) with the file-system owner or group of
# the requested file.
#LoadModule authz_owner_module /usr/lib64/httpd/modules/mod_authz_owner.so

# Provides authorization capabilities so that authenticated users can be
# allowed or denied access to portions of the web site by group membership.
# Similar functionality is provided by mod_authz_groupfile and
# mod_authz_dbm, with the exception that this module queries a SQL database
# to determine whether a user is a member of a group.
#LoadModule authz_dbd_module /usr/lib64/httpd/modules/mod_authz_dbd.so

# Provides core authorization capabilities so that authenticated users can
# be allowed or denied access to portions of the web site. mod_authz_core
# provides the functionality to register various authorization providers.
LoadModule authz_core_module /usr/lib64/httpd/modules/mod_authz_core.so

# Allows authentication front-ends such as mod_auth_basic to authenticate
# users through an ldap directory.
#LoadModule authnz_ldap_module /usr/lib64/httpd/modules/mod_authnz_ldap.so

# The directives provided by mod_access_compat are used in <Directory>,
# <Files>, and <Location> sections as well as .htaccess files to control
# access to particular parts of the server.
LoadModule access_compat_module /usr/lib64/httpd/modules/mod_access_compat.so

# Allows the use of HTTP Basic Authentication to restrict access by
# looking up users in the given providers. HTTP Digest Authentication
# is provided by mod_auth_digest.
LoadModule auth_basic_module /usr/lib64/httpd/modules/mod_auth_basic.so

# Allows the use of an HTML login form to restrict access by looking up
# users in the given providers.
#LoadModule auth_form_module /usr/lib64/httpd/modules/mod_auth_form.so

# Implements HTTP Digest Authentication (RFC2617), and provides an
# alternative to mod_auth_basic where the password is not transmitted
# as cleartext.
#LoadModule auth_digest_module /usr/lib64/httpd/modules/mod_auth_digest.so

# Makes it easy to restrict what HTTP methods can be used on a server.
#LoadModule allowmethods_module /usr/lib64/httpd/modules/mod_allowmethods.so

# Caching frequently requested files that change very infrequently is a
# technique for reducing server load. mod_file_cache provides two techniques
# for caching frequently requested static files.
#LoadModule file_cache_module /usr/lib64/httpd/modules/mod_file_cache.so

# mod_cache implements an RFC 2616 compliant HTTP content caching filter,
# with support for the caching of content negotiated responses containing
# the Vary header.
#LoadModule cache_module /usr/lib64/httpd/modules/mod_cache.so

# Implements a disk based storage manager for mod_cache.
#LoadModule cache_disk_module /usr/lib64/httpd/modules/mod_cache_disk.so

# Implements a shared object cache (socache) based storage manager for mod_cache.
#LoadModule cache_socache_module /usr/lib64/httpd/modules/mod_cache_socache.so

# Shared object cache provider which provides for creation and access to a
# cache backed by a high-performance cyclic buffer inside a shared
# memory segment.
#LoadModule socache_shmcb_module /usr/lib64/httpd/modules/mod_socache_shmcb.so

# Shared object cache provider which provides for creation and access to a
# cache backed by a DBM database.
#LoadModule socache_dbm_module /usr/lib64/httpd/modules/mod_socache_dbm.so

# Shared object cache provider which provides for creation and access to a
# cache backed by the memcached high-performance, distributed memory object
# caching system.
#LoadModule socache_memcache_module /usr/lib64/httpd/modules/mod_socache_memcache.so

# Defines programmatic hooks for other modules to periodically run tasks.
#LoadModule watchdog_module /usr/lib64/httpd/modules/mod_watchdog.so

# Provides macros within Apache httpd runtime configuration files, to ease the
# process of creating numerous similar configuration blocks.
#LoadModule macro_module /usr/lib64/httpd/modules/mod_macro.so

# Manages SQL database connections using APR.
#LoadModule dbd_module /usr/lib64/httpd/modules/mod_dbd.so

# Allows for the logging of all input received by Apache and/or all output
# sent by Apache to be logged (dumped) to the error.log file.
#LoadModule dumpio_module /usr/lib64/httpd/modules/mod_dumpio.so

# Provides the ability to buffer the input and output filter stacks
#LoadModule buffer_module /usr/lib64/httpd/modules/mod_buffer.so

# Convert response body into an RFC2397 data URL
#LoadModule data_module /usr/lib64/httpd/modules/mod_data.so

# Bandwidth Rate Limiting for Clients
#LoadModule ratelimit_module /usr/lib64/httpd/modules/mod_ratelimit.so

# Set timeout and minimum data rate for receiving requests
#LoadModule reqtimeout_module /usr/lib64/httpd/modules/mod_reqtimeout.so

# Pass the response body through an external program before delivery
# to the client
#LoadModule ext_filter_module /usr/lib64/httpd/modules/mod_ext_filter.so

# Filters to handle and make available HTTP request bodies
#LoadModule request_module /usr/lib64/httpd/modules/mod_request.so

# Server-parsed html documents (Server Side Includes)
#LoadModule include_module /usr/lib64/httpd/modules/mod_include.so

# Context-sensitive smart filter configuration module
LoadModule filter_module /usr/lib64/httpd/modules/mod_filter.so

# Reflect a request body as a response via the output filter stack.
#LoadModule reflector_module /usr/lib64/httpd/modules/mod_reflector.so

# Perform search and replace operations on response bodies
#LoadModule substitute_module /usr/lib64/httpd/modules/mod_substitute.so

# Filter Input (request) and Output (response) content using sed syntax
#LoadModule sed_module /usr/lib64/httpd/modules/mod_sed.so

# Specify character set translation or recoding
#LoadModule charset_lite_module /usr/lib64/httpd/modules/mod_charset_lite.so

# Compress content before it is delivered to the client
LoadModule deflate_module /usr/lib64/httpd/modules/mod_deflate.so

# Enhanced charset/internationalisation support for libxml2-based
# filter modules
#LoadModule xml2enc_module /usr/lib64/httpd/modules/mod_xml2enc.so

# Rewrite HTML links in to ensure they are addressable from Clients'
# networks in a proxy context.
#LoadModule proxy_html_module /usr/lib64/httpd/modules/mod_proxy_html.so
LoadModule mime_module /usr/lib64/httpd/modules/mod_mime.so

# LDAP connection pooling and result caching services
#LoadModule ldap_module /usr/lib64/httpd/modules/mod_ldap.so

# Logging of the requests made to the server
LoadModule log_config_module /usr/lib64/httpd/modules/mod_log_config.so

# Additional configurable debug logging
#LoadModule log_debug_module /usr/lib64/httpd/modules/mod_log_debug.so

# Forensic Logging of the requests made to the server
#LoadModule log_forensic_module /usr/lib64/httpd/modules/mod_log_forensic.so

# Logging of input and output bytes per request
#LoadModule logio_module /usr/lib64/httpd/modules/mod_logio.so

# Provides Lua hooks into various portions of the httpd request processing
#LoadModule lua_module /usr/lib64/httpd/modules/mod_lua.so

# Modifies the environment which is passed to CGI scripts and SSI pages
LoadModule env_module /usr/lib64/httpd/modules/mod_env.so

# Determines the MIME type of a file by looking at a few bytes of its contents
LoadModule mime_magic_module /usr/lib64/httpd/modules/mod_mime_magic.so

# Generation of Expires and Cache-Control HTTP headers according to
# user-specified criteria
LoadModule expires_module /usr/lib64/httpd/modules/mod_expires.so

# Customization of HTTP request and response headers
LoadModule headers_module /usr/lib64/httpd/modules/mod_headers.so

# Clickstream logging of user activity on a site
#LoadModule usertrack_module /usr/lib64/httpd/modules/mod_usertrack.so

# Provides an environment variable with a unique identifier for each request
#LoadModule unique_id_module /usr/lib64/httpd/modules/mod_unique_id.so

# Allows the setting of environment variables based on characteristics
# of the request
LoadModule setenvif_module /usr/lib64/httpd/modules/mod_setenvif.so

# Version dependent configuration
#LoadModule version_module /usr/lib64/httpd/modules/mod_version.so

# Replaces the original client IP address for the connection with the
# useragent IP address list presented by a proxies or a load balancer
# via the request headers.
LoadModule remoteip_module /usr/lib64/httpd/modules/mod_remoteip.so

# Multi-protocol proxy/gateway server
LoadModule proxy_module /usr/lib64/httpd/modules/mod_proxy.so

# mod_proxy extension for CONNECT request handling
#LoadModule proxy_connect_module /usr/lib64/httpd/modules/mod_proxy_connect.so

# FTP support module for mod_proxy
#LoadModule proxy_ftp_module /usr/lib64/httpd/modules/mod_proxy_ftp.so

# HTTP support module for mod_proxy
#LoadModule proxy_http_module /usr/lib64/httpd/modules/mod_proxy_http.so

# FastCGI support module for mod_proxy
LoadModule proxy_fcgi_module /usr/lib64/httpd/modules/mod_proxy_fcgi.so

# SCGI gateway module for mod_proxy
#LoadModule proxy_scgi_module /usr/lib64/httpd/modules/mod_proxy_scgi.so

# fdpass external process support module for mod_proxy
#LoadModule proxy_fdpass_module /usr/lib64/httpd/modules/mod_proxy_fdpass.so

# Websockets support module for mod_proxy
#LoadModule proxy_wstunnel_module /usr/lib64/httpd/modules/mod_proxy_wstunnel.so

# AJP support module for mod_proxy
#LoadModule proxy_ajp_module /usr/lib64/httpd/modules/mod_proxy_ajp.so

# mod_proxy extension for load balancing
#LoadModule proxy_balancer_module /usr/lib64/httpd/modules/mod_proxy_balancer.so

# Dynamic mass reverse proxy extension for mod_proxy
#LoadModule proxy_express_module /usr/lib64/httpd/modules/mod_proxy_express.so

# Session support
#LoadModule session_module /usr/lib64/httpd/modules/mod_session.so

# Cookie based session support
#LoadModule session_cookie_module /usr/lib64/httpd/modules/mod_session_cookie.so

# Session encryption support
#LoadModule session_crypto_module /usr/lib64/httpd/modules/mod_session_crypto.so

# DBD/SQL based session support
#LoadModule session_dbd_module /usr/lib64/httpd/modules/mod_session_dbd.so

# Slot-based shared memory provider
LoadModule slotmem_shm_module /usr/lib64/httpd/modules/mod_slotmem_shm.so
LoadModule slotmem_plain_module /usr/lib64/httpd/modules/mod_slotmem_plain.so

# Enable SSL encryption for NetWare
#LoadModule ssl_module /usr/lib64/httpd/modules/mod_ssl.so

# Send static content at a bandwidth rate limit, defined by the various
# old modem standards
#LoadModule dialup_module /usr/lib64/httpd/modules/mod_dialup.so

# Request Counting load balancer scheduler algorithm
#LoadModule lbmethod_byrequests_module /usr/lib64/httpd/modules/mod_lbmethod_byrequests.so

# Weighted Traffic Counting load balancer scheduler algorithm
#LoadModule lbmethod_bytraffic_module /usr/lib64/httpd/modules/mod_lbmethod_bytraffic.so

# Pending Request Counting load balancer scheduler algorithm
#LoadModule lbmethod_bybusyness_module /usr/lib64/httpd/modules/mod_lbmethod_bybusyness.so

# Heartbeat Traffic Counting load balancer scheduler algorithm
#LoadModule lbmethod_heartbeat_module /usr/lib64/httpd/modules/mod_lbmethod_heartbeat.so

# Basic (required) security for Unix-family platforms
LoadModule unixd_module /usr/lib64/httpd/modules/mod_unixd.so

# Sends messages with server status to frontend proxy
#LoadModule heartbeat_module /usr/lib64/httpd/modules/mod_heartbeat.so

# Centralized monitor for mod_heartbeat origin servers
#LoadModule heartmonitor_module /usr/lib64/httpd/modules/mod_heartmonitor.so

# Distributed Authoring and Versioning (WebDAV) functionality
LoadModule dav_module /usr/lib64/httpd/modules/mod_dav.so

# Provides information on server activity and performance
LoadModule status_module /usr/lib64/httpd/modules/mod_status.so

# Generates directory indexes, automatically, similar to the Unix ls command
# or the Win32 dir shell command
LoadModule autoindex_module /usr/lib64/httpd/modules/mod_autoindex.so

# Sends files that contain their own HTTP headers
#LoadModule asis_module /usr/lib64/httpd/modules/mod_asis.so

# Provides a comprehensive overview of the server configuration
#LoadModule info_module /usr/lib64/httpd/modules/mod_info.so

# Allows CGI scripts to run as a specified user and Group
LoadModule suexec_module /usr/lib64/httpd/modules/mod_suexec.so

<IfModule mpm_prefork_module>
	#LoadModule cgi_module /usr/lib64/httpd/modules/mod_cgi.so
</IfModule>
<IfModule !mpm_prefork_module>
	#LoadModule cgid_module /usr/lib64/httpd/modules/mod_cgid.so
</IfModule>

# Filesystem provider for mod_dav
LoadModule dav_fs_module /usr/lib64/httpd/modules/mod_dav_fs.so

# Generic locking module for mod_dav
#LoadModule dav_lock_module /usr/lib64/httpd/modules/mod_dav_lock.so

# Provides for dynamically configured mass virtual hosting
LoadModule vhost_alias_module /usr/lib64/httpd/modules/mod_vhost_alias.so

# Provides for content negotiation
LoadModule negotiation_module /usr/lib64/httpd/modules/mod_negotiation.so

# Provides for "trailing slash" redirects and serving directory index files
LoadModule dir_module /usr/lib64/httpd/modules/mod_dir.so

# Execute CGI scripts based on media type or request method
#LoadModule actions_module /usr/lib64/httpd/modules/mod_actions.so

# Attempts to correct mistaken URLs by ignoring capitalization, or
# attempting to correct various minor misspellings.
#LoadModule speling_module /usr/lib64/httpd/modules/mod_speling.so

# User-specific directories
#LoadModule userdir_module /usr/lib64/httpd/modules/mod_userdir.so

# Provides for mapping different parts of the host filesystem in the
# document tree and for URL redirection
LoadModule alias_module /usr/lib64/httpd/modules/mod_alias.so

# Rewrite HTML links in to ensure they are addressable from Clients
# networks in a proxy context.
LoadModule rewrite_module /usr/lib64/httpd/modules/mod_rewrite.so

###############################################################################

ServerRoot "/etc/httpd"
Listen 80

<IfModule unixd_module>
    User apache
    Group apache
</IfModule>

EnableMMAP on
EnableSendfile on

###############################################################################

ServerName localhost
ServerAdmin root@localhost

DocumentRoot "/var/www/html"

<Directory "/var/www/html">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>

<IfModule dir_module>
    DirectoryIndex index.html
</IfModule>

<Files ".ht*">
    Require all denied
</Files>

###############################################################################

<IfModule mime_module>
    TypesConfig conf/mime.types

    AddType application/x-compress .Z
    AddType application/x-gzip .gz .tgz
</IfModule>

<IfModule log_config_module>
    LogFormat "%h %l %u %t \"%r\" %>s %b" common
    LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\"" combined

    <IfModule logio_module>
      LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\" %I %O" combinedio
    </IfModule>

    CustomLog "/var/log/httpd/access.log" combined
</IfModule>

ErrorLog "/var/log/httpd/error.log"
LogLevel notice

###############################################################################

# Server-pool management (MPM specific)
Include conf/extra/httpd-mpm.conf

# Multi-language error messages
#Include conf/extra/httpd-multilang-errordoc.conf

# Fancy directory listings
Include conf/extra/httpd-autoindex.conf

# Language settings
Include conf/extra/httpd-languages.conf

# User home directories
#Include conf/extra/httpd-userdir.conf

# Real-time info on requests and configuration
Include conf/extra/httpd-info.conf

# Various default settings
Include conf/extra/httpd-default.conf

# Configure SSL/TLS global settings
#Include conf/extra/httpd-ssl.conf

# Extended configurations
IncludeOptional conf.d/*.conf

# Virtual hosts
IncludeOptional vhost.d/*.conf

###############################################################################

